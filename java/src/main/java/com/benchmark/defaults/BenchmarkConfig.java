package com.benchmark.defaults;

/**
 * Defines the number of warmup and measurement iterations used for the JMH benchmark runs.
 */
public final class BenchmarkConfig {
  public static final int WARMUP_ITERATIONS = 3;
  public static final int MEASUREMENT_ITERATIONS = 7;
}
